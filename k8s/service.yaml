apiVersion: v1
kind: Service
metadata:
  name: deployment-stable-service
spec:
  type: LoadBalancer
  selector:
    app: popcorn-api

  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP