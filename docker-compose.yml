services:
  db:
    image: postgres:15
    container_name: popcorn_dev_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: popcorn_dev
    ports:
      - '5432:5432'
